import { TAPAgent } from '../agent';
import { Cancel } from '@taprsvp/types';
import { BaseMessageObject } from './base-message';

/**
 * Cancel message object with fluent response interface
 */
export class CancelObject extends BaseMessageObject {
  // No additional methods for now
}